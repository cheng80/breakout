# 아이템 제작 체크리스트

아이템을 추가하거나 효과·수치·연출을 수정할 때 코드와 이 문서를 같은 커밋에서 함께 갱신한다. 새 아이템은 구현 전에 미완료 항목으로 추가하고, 게임 규칙·화면·검증이 끝난 뒤 `[x]`로 변경한다.

## 화면 배치 아이템

- [x] 공 추가 (`multiball`) — 획득 순간 충돌한 공의 진행 반대쪽에 추가 공 1개가 같은 속도로 꼬리처럼 붙어 현재 공격에 합류하고, 다음 발사 공 수도 1개 증가한다. 공 상한은 10스테이지마다 10개씩 늘어나며 최대 100개.
- [x] 폭탄 (`bomb`) — 주변 3×3 범위의 파괴 가능한 블록에 피해 1을 준다.
- [x] 보호막 (`shield`) — 블록 하강으로 게임오버될 때 한 번 되돌린다.
- [x] 강화볼 ×2 (`power`) — 2회 발사 동안 공의 피해를 2배로 만든다.
- [x] 강화볼 ×3 (`power3`) — 2회 발사 동안 공의 피해를 3배로 만든다.
- [x] 강화볼 ×4 (`power4`) — 2회 발사 동안 공의 피해를 4배로 만든다.
- [x] 공 감소 함정 (`trap`) — 공을 1개 줄인다. 최소 1개는 유지한다.
- [x] 블랙홀 (`blackhole`) — 주변 공의 궤도를 휘게 하고 충전 수만큼 흡수하며 5초마다 안전한 빈칸으로 이동한다.

## 특수 블록

- [x] 레이저 블록 (`laser`) — 파괴되면 같은 가로줄의 파괴 가능한 블록을 모두 제거한다.
- [x] 강철 블록 (`steel`) — 모든 공격에 파괴되지 않고 공만 반사한다.

## 보관형 궁극기

궁극기는 발사 대기 상태에서 슬롯을 누른 뒤 게임판 위치를 선택해 사용한다. 유효한 대상이 없으면 소비하지 않으며 강철 블록은 모든 궁극기에 면역이다.
기본 연출은 PixiJS의 도형·선·블러·투명도로 구현하고, 효과만으로 식별하기 어려울 때 아이콘 외 추가 이미지 리소스를 제작한다.
효과음은 기존 사운드를 재사용한다. 레이저 계열은 `laser`, 그 외 대부분의 대규모 파괴 궁극기는 `bomb`을 사용한다.
궁극기 피해는 사용 즉시가 아니라 연출이 실제로 명중하는 순간 적용한다. 연쇄 번개·핵융합 연쇄·미사일 포화는 각 전류·폭발·탄두가 대상에 닿을 때 해당 블록만 순차적으로 제거한다.

보상은 선택지가 아닌 활약 별 기반 가중치 랜덤으로 1개만 지급한다. 별 1개는 지급하지 않고, 별 2/3/4/5개는 각각 20%/50%/80%/100% 확률로 지급한다. `연쇄 번개`와 `운석 충돌`은 낮은 별에서도 잘 나오는 입문형, `블랙홀 붕괴`와 `십자포화`는 중간형, `반물질 폭탄`·`궤도 레이저`는 상위형, `핵융합 연쇄`·`미사일 포화`는 높은 별에서 주로 나오는 희귀형으로 조정한다.

- [x] 반물질 폭탄 (`antimatter`) — 선택 지점을 중심으로 5×5 범위의 파괴 가능한 블록을 체력과 무관하게 제거한다. 블록 파편과 보라색 고리가 암흑 코어로 수축한 뒤 백색 섬광, 구형 충격파, 블러가 적용된 불규칙한 에너지 균열로 소멸하며 `public/images/ultimates/antimatter.png` 아이콘과 폭탄 효과음을 사용한다.
- [x] 궤도 레이저 (`orbitalLaser`) — 선택 지점 주변의 연속 3열을 화면 가장자리에서도 유지해 파괴 가능한 블록을 체력과 무관하게 제거한다. 조준선과 상단 충전 고리 뒤 세 열 폭의 다층 광선이 전체 높이를 관통하며 `public/images/ultimates/orbital-laser.png` 아이콘과 레이저 효과음을 사용한다.
- [x] 연쇄 번개 (`chainLightning`) — 선택 지점에서 가까운 파괴 가능한 블록 최대 12개를 체력과 무관하게 제거한다. 푸른 외곽광과 노란 중심선의 지그재그 전류가 잔가지를 뻗으며 목표 사이를 연쇄하고 `public/images/ultimates/chain-lightning.png` 아이콘과 폭탄 효과음을 사용한다.
- [x] 운석 충돌 (`meteorImpact`) — 운석이 선택 지점에 실제 충돌하는 순간 중심부 블록을 제거하고 외곽 블록에 피해를 준다. 암석 본체가 다중 화염 꼬리와 불씨를 흩리며 대각선으로 낙하한 뒤 피해·폭탄 효과음과 동시에 파편·섬광·충격파를 만들며 `public/images/ultimates/meteor-impact.png` 아이콘을 사용한다.
- [x] 블랙홀 붕괴 (`blackHoleCollapse`) — 선택 범위의 블록을 한 점으로 끌어당긴 뒤 폭발시켜 제거한다. 실제 블록과 체력 표시가 나선 궤적을 따라 이동·축소되어 사건의 지평선에 흡수되고, 강착 원반이 붕괴한 뒤 청색 양방향 제트와 타원 충격파가 방출되며 `public/images/ultimates/black-hole-collapse.png` 아이콘과 폭탄 효과음을 사용한다.
- [x] 십자포화 (`crossfire`) — 선택 위치를 중심으로 가로 3줄과 세로 3줄의 블록을 제거한다. 얇은 조준 격자 뒤 화면 네 방향에서 3줄씩의 다층 레이저가 진입해 중앙에서 교차하며 `public/images/ultimates/crossfire.png` 아이콘과 레이저 효과음을 사용한다.
- [x] 핵융합 연쇄 (`fusionChain`) — 선택 블록에서 시작해 주변 블록으로 폭발을 전염시킨다. 목표 블록이 순서대로 백색 점화된 뒤 주황색 국소 폭발과 방사형 불꽃을 만들며 `public/images/ultimates/fusion-chain.png` 아이콘과 폭탄 효과음을 사용한다.
- [x] 미사일 포화 (`missileBarrage`) — 다수의 파괴 가능한 블록을 자동 조준해 동시에 공격한다. 화면 상단과 좌우에서 실제 탄두·동체·날개·화염 꼬리를 가진 미사일이 곡선으로 진입해 목표별로 폭발하며 `public/images/ultimates/missile-barrage.png` 아이콘과 폭탄 효과음을 사용한다.
